<Window x:Class="DefectListWpfControl.DefectList.Views.AddBomHeaderView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DefectListWpfControl.DefectList.Views"
        xmlns:components="clr-namespace:DefectListWpfControl.DefectList.Components"
        mc:Ignorable="d"
        Title="{Binding FormName}" Height="570" Width="550" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0"
                   Style="{StaticResource PageHeader}"
                   Text="{Binding FormName}" />

        <components:BomHeaderDetailsForm Grid.Row="1"
                                        Margin="10 20 10 0"
                                        DataContext="{Binding BomHeaderDetailsFormViewModel}"/>
    </Grid>
</Window>
