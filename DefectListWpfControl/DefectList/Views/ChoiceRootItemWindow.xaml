<Window x:Class="DefectListWpfControl.DefectList.Views.ChoiceRootItemWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DefectListWpfControl.DefectList.Views"
        xmlns:components="clr-namespace:DefectListWpfControl.DefectList.Components"
        mc:Ignorable="d"
        Title="Выбор ремонтного изделия" Height="500" Width="600" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Menu Height="25" VerticalAlignment="Top" Background="#efefef">
                    <MenuItem Header="Действие">
                        <MenuItem Header="Обновить ремонтные изделия" Command="{Binding LoadRootItemsCommand}"/>
                        <Separator Visibility="Visible"/>
                        <MenuItem Header="Добавить ремонтное изделие" Command="{Binding OpenAddRootItemsFormCommand}" />
                        <MenuItem Header="Удалить выделенное ремонтное изделие" Command="{Binding DeleteRootItemsCommand}" />
                    </MenuItem>
                </Menu>
            </Grid>

            <Grid Grid.Row="1">
                <TextBlock Text="Ремонтные изделия" HorizontalAlignment="Center" FontWeight="Bold" Padding="5" />
            </Grid>

            <Grid Grid.Row="2">
                <Border BorderBrush="{StaticResource BorderPrimary}"
                        BorderThickness="1"
                        CornerRadius="5"
                        Padding="20"
                        SnapsToDevicePixels="True">
                    <StackPanel VerticalAlignment="Center">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility"
                                        Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLoading}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <components:DataLoadingSpinner DataContext="Загрузка ремонтных изделий ..." />
                    </StackPanel>
                </Border>
                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility"
                                    Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}"
                                             Value="False">
                                    <Setter Property="Visibility"
                                            Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <Grid>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility"
                                        Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasErrorMessage}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>

                        <TextBlock Text="{Binding ErrorMessage}" Style="{StaticResource ErrorMessage}" />
                    </Grid>

                    <Grid>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility"
                                        Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasErrorMessage}"
                                                 Value="False">
                                        <Setter Property="Visibility"
                                                Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>

                        <DataGrid
                            ItemsSource="{Binding RootItemViewModels}"
                            SelectedItem="{Binding SelectedRootItemViewModel}"
                            IsSynchronizedWithCurrentItem="True"
                            CanUserAddRows="False"
                            AutoGenerateColumns="False"
                            IsReadOnly="True"
                            SelectionMode="Single"
                            SelectionUnit="FullRow"
                            HorizontalAlignment="Left"
                            ColumnWidth="*">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Обозначение" Binding="{Binding RootItem.Izdel}" />
                                <DataGridTextColumn Header="Наименование" Binding="{Binding RootItem.IzdelIma}" />
                                <DataGridTextColumn Header="Тип" Binding="{Binding RootItem.IzdelTyp}" />
                                <DataGridTextColumn Header="Исходное обозначение" Binding="{Binding RootItem.IzdelInitial}" />
                            </DataGrid.Columns>
                            <DataGrid.InputBindings>
                                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding RootItemDoubleClickCommand}" />
                            </DataGrid.InputBindings>
                        </DataGrid>
                    </Grid>
                </Grid>
            </Grid>

            <Grid Grid.Row="3" Margin="0 10 0 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button
                    Grid.Column="0"
                    Content="Выбрать"
                    Width="100"
                    Style="{StaticResource PrimaryButton}"
                    IsEnabled="{Binding CanSubmit}"
                    Command="{Binding ChoiceRootItemCommand}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
