<Window x:Class="DefectListWpfControl.DefectList.Views.DefectListHeaderFilterModeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:DefectListWpfControl.DefectList.ViewModels"
        mc:Ignorable="d"
        Title="Настройка фильтра"
        Height="230"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        d:DataContext="{d:DesignInstance d:Type=viewModels:DefectListHeaderFilterModeViewModel}"
        Closing="Window_OnClosing">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Row="0" Margin="0 0 0 10">
            <TextBlock Text="Выбор режима фильтрации" Style="{StaticResource PageHeader}" />
        </Grid>
        <StackPanel Grid.Row="1">
            <ListBox ItemsSource="{Binding FilterModes}"
                     SelectionMode="Single"
                     FontSize="14"
                     BorderThickness="0"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <RadioButton
                                Grid.Column="0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                GroupName="FilterModesGroup"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Text}"
                                       HorizontalAlignment="Stretch"
                                       Width="Auto"
                                       Padding="5"
                                       Cursor="Hand"
                                       VerticalAlignment="Center">
                                <TextBlock.InputBindings>
                                    <MouseBinding MouseAction="LeftClick" Command="{Binding ClickCommand}" />
                                </TextBlock.InputBindings>
                            </TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.Resources>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.Resources>
            </ListBox>
        </StackPanel>
        <Grid Row="2" Margin="0 10">
            <Button Content="Выбрать" Width="150" Click="Button_Click" Style="{StaticResource PrimaryButton}"/>
        </Grid>
    </Grid>
</Window>
